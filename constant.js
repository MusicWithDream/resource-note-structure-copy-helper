const IGNORE_FILE_LiST_DEFAULT = [
  'node_modules',
  'LICENSE',
];

const IGNORE_TYPE_SET_DEFAULT = new Set(['.md', '.json', '.yaml', '.d.ts']);

module.exports = {
  IGNORE_FILE_LiST_DEFAULT,
  IGNORE_TYPE_SET_DEFAULT,
};
